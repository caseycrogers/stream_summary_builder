name: stream_summary_builder
description: A new Flutter package.
version: 0.1.0-dev.1
homepage: 'https://github.com/caseycrogers/stream_summary_builder'

environment:
  sdk: '>=2.12.0-0 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: